{
  "name": "suno-forge",
  "version": "4.1.0",
  "private": true,
  "description": "A no-scroll, all-at-once Suno style tag wall.",
  "type": "module",
  "scripts": {
    "test": "node --check data.js && node --check prompt-app.js && node --check sw.js && node scripts/check-project.mjs && node scripts/test-ui.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
