{
  "name": "suno-forge",
  "version": "3.0.3",
  "private": true,
  "description": "A fast, local-first AI music prompt studio.",
  "type": "module",
  "scripts": {
    "test": "node --check data.js && node --check prompt-app.js && node --check sw.js && node scripts/check-project.mjs && node scripts/test-ui.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
