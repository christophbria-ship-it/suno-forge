{
  "name": "suno-forge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm test",
    "test": "node --check data.js && node --check prompt-app.js && node --check sw.js"
  },
  "engines": {
    "node": "24.x"
  }
}
