{
  "name": "suno-forge",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --check data.js && node --check app-core.js && node --check app-editor.js && node --check app-actions.js && node --check app-storage.js && node --check api/generate-lyrics.js"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module"
}
